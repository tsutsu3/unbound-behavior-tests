<!-- generated by gen.py. DO NOT EDIT.
     unbound: 1.26.0 (release-1.26.0, a45da353)
     cases: d3ac0e6 -->


# Appendix: behaviour confirmed from the implementation (Unbound 1.26.0)

The category is a claim about the man page. A = not described in the man page, B = described, but the description disagrees with the implementation, C = the description matches the implementation but the result is counter-intuitive, undecided = not decided yet.

| Category | Section | Behaviour | Evidence (source) | man | Status | Case |
| --- | --- | --- | --- | --- | --- | --- |
| A | 5.2 | An include: wildcard under a missing directory is an error that names the pattern itself as the file | util/configlexer.lex config_start_include_glob() passes GLOB_ERR to glob(), so a directory that cannot be opened aborts the glob with an error other than GLOB_NOMATCH. That branch hands the unexpanded pattern to config_start_include() (let original deal with it), which tries to open a file literally named conf.d/*.conf and fails | doc/unbound.conf.5.in INCLUDING FILES: Wildcards can be used to include multiple files, see glob(7). (nothing about a pattern whose directory is missing) | verified | `conf-include-glob-missing-directory-is-error` |
| A | 5.2 | An include: wildcard that matches no file in an existing directory is silently ignored | util/configlexer.lex config_start_include_glob() calls glob() with GLOB_ERR when the name contains a wildcard character (*, ?, [, { or ~). If the directory exists but nothing matches, glob() returns GLOB_NOMATCH and the function returns without a word. A name without wildcards goes straight to config_start_include(), which reports a file it cannot open | doc/unbound.conf.5.in INCLUDING FILES: Wildcards can be used to include multiple files, see glob(7). (nothing about a pattern that matches nothing) | verified | `conf-include-glob-no-match-is-silent` |
| A | 5.2 | include: wildcards are read in sorted file name order, and the last file wins | util/configlexer.lex config_start_include_glob() does not set GLOB_NOSORT (there is a comment: do not set GLOB_NOSORT so the results are sorted and in a predictable order). It then pushes the files onto the include stack in reverse with for(i=(int)g.gl_pathc-1; i>=0; i--), so they are popped in sorted order. Head insertion in cfg_str2list_insert() then makes the line read last win | doc/unbound.conf.5.in INCLUDING FILES: Wildcards can be used to include multiple files, see glob(7). (nothing about the read order) | verified | `conf-include-glob-processed-in-sorted-order` |
| A | 5.2 | An included file is placed at the include: position, and lines written after it win | util/configlexer.lex config_start_include() swaps the input buffer with yy_switch_to_buffer() and config_end_include() restores it at EOF, so as a token stream the file is expanded exactly at the include: position. util/config_file.c cfg_str2list_insert() then inserts at the head, so the line read later comes first in cfg->local_zones, and services/localzone.c lz_enter_zones() registers it first, so it wins | doc/unbound.conf.5.in INCLUDING FILES: Processing continues as if the text from the included file was copied into the config file at that point. | verified | `conf-include-later-line-wins` |
| A | 5.2 | With include: at the end, the included file beats the main configuration | util/configlexer.lex expands at the textual position, so no distinction between the main file and the included file survives. Head insertion in util/config_file.c cfg_str2list_insert() combined with services/localzone.c lz_enter_zone_dname() returning the existing zone on a duplicate means that only the position in the file decides the winner | doc/unbound.conf.5.in INCLUDING FILES: It can appear anywhere, it accepts a single file name as argument. Processing continues as if the text from the included file was copied into the config file at that point. | verified | `conf-include-position-not-file-decides` |
| A | 5.2 | After include-toplevel: a section must be opened again, or it is a syntax error | The include-toplevel rule in util/configlexer.lex returns VAR_FORCE_TOPLEVEL after config_start_include_glob(yytext, 1), and returns the same token at the included file's EOF when prev_toplevel is set. util/configparser.y force_toplevel sets cfg_parser->started_toplevel = 0, so the next directive is outside any section. The error text is a dedicated one that util/config_file.c ub_c_error() substitutes when started_toplevel == 0 | doc/unbound.conf.5.in INCLUDING FILES: For a more structural include option, the include-toplevel: directive can be used. This closes whatever section clause is currently active (if any) and forces the use of section clauses in the included files and right after this directive. | verified | `conf-include-toplevel-closes-section` |
| A | 5.2 | The lexer does not resolve backslash escapes and hands them to the RR parser | util/configlexer.lex. DQANY/SQANY/UNQUOTEDLETTER only accept \\. as one two-character unit, and yytext is strdup()ed unchanged. The escape is resolved later, in sldns/parseutil.c sldns_parse_char() | none (the FILE FORMAT section says nothing about backslashes; searched for 'backslash' and 'escape') | verified | `conf-lexer-backslash-not-resolved` |
| A | 5.2 | A double quote inside a double-quoted value ends the value there | The util/configlexer.lex `<quotedstring>\"` rule. DQANY = [^\"\n\r\\]\|\\. so a " without a backslash before it closes the string | none (the FILE FORMAT section of `unbound.conf.5.in` covers only whitespace, comments and colons, and says nothing about quoting) | verified | `conf-lexer-double-quote-terminates-early` |
| A | 5.2 | Forgetting the second argument of local-zone makes it consume the next line's directive name | util/configlexer.lex. YDVAR(2, VAR_LOCAL_ZONE) enters the val state with num_args=2, and newlines are skipped by <INITIAL,val>{NEWLINE}. UNQUOTEDLETTER includes the colon, so `local-data:` on the next line matches <val>{UNQUOTEDLETTER}* and becomes the second STRING_ARG | none (the FILE FORMAT section does not say what happens when arguments are missing) | verified | `conf-lexer-missing-argument-eats-next-directive` |
| A | 5.2 | A quoted value cannot span lines (there is no line continuation syntax) | The util/configlexer.lex `<quotedstring>{NEWLINE}` rule emits yyerror("newline inside quoted string, no end \"") and returns to INITIAL. DQANY does not include NEWLINE either | none (the FILE FORMAT section mentions neither line continuation nor that a quoted value must fit on one line) | verified | `conf-lexer-no-line-continuation` |
| A | 5.2 | Inside a local-data value, ; starts a comment, not # | sldns/parse.c sldns_bget_token_par(). When c == ';', it is not quoted and the previous character is not \\, com=1 is set and the rest of the line is replaced with whitespace. The config file's # comment is {SPACE}*{COMMENT}.* in util/configlexer.lex and does not apply inside quotes | none (FILE FORMAT in the man page describes only # as a comment, and does not mention that a local-data value, being RR text, has a separate comment rule) | verified | `local-data-semicolon-starts-rr-comment` |
| A | 5.3 | On a duplicate only the local-zone line is discarded; all the local-data goes into one zone | services/localzone.c lz_enter_zones() discards the duplicate zone, but lz_enter_data() walks cfg->local_data and lz_enter_rr_str() looks up the covering zone for each record, so the local-data from both blocks remains | none (the man page says nothing about duplicate local-zone) | verified | `local-zone-duplicate-keeps-both-local-data` |
| A | 5.3 | With duplicate local-zone lines, the type written last in the file remains | util/config_file.c cfg_str2list_insert() inserts at the head, so cfg->local_zones is in reverse order. services/localzone.c lz_enter_zones() registers from the head, and lz_enter_zone_dname() does log_warn and returns the existing zone on a duplicate, so the one registered first, which is the line written last, remains | none (`unbound.conf.5.in` describes neither how a duplicate local-zone is handled nor the storage order of a repeated directive; searched for 'duplicate', 'twice' and 'last one') | verified | `local-zone-duplicate-last-line-wins` |
| A | 5.3 | In an implicit transparent zone, a query for the same name with another type gets NODATA | services/localzone.c lz_setup_implicit() creates a local_zone_transparent zone. transparent returns NOERROR/NODATA when the name exists but the type does not | doc/unbound.conf.5.in, transparent entry: If the query is for a name given in local-data but no such type of data is given in localdata, then a NOERROR NODATA answer is returned. | verified | `local-zone-implicit-transparent-gives-nodata` |
| A | 5.3 | local-zone names ignore the trailing dot and letter case, so different spellings are duplicates of one zone | services/localzone.c parse_dname() → sldns/str2wire.c sldns_str2wire_dname_buf_rel(). If the last character is not . the branch /* add root label if last char was not '.' */ adds the root label, so example.com and example.com. are the same wire name. The zone tree key comparison local_zone_cmp() uses dname_lab_cmp(), which ignores case. So rbtree_insert() detects the duplicate, and lz_enter_zone_dname() logs log_warn("duplicate local-zone %s") and returns the existing zone | none (the local-zone: entry in `unbound.conf.5.in` says neither whether the trailing dot is required nor how case is handled) | verified | `local-zone-name-trailing-dot-and-case-are-same-zone` |
| A | 5.3 | A deny zone at the root does not reach names under a more specific zone; the built-in zones keep answering | services/localzone.c local_zones_tags_lookup() finds the closest enclosing zone with rbtree_find_less_equal() and walks up the parents, so a query is answered by the most specific zone that covers it. local_zone_enter_defaults() adds the built-in zones unless lz_exists() finds a zone with exactly the same name, and a root zone is never the same name, so test. stays in the tree below . | none (the local-zone: entry does not say which zone answers when zones are nested; the closest-match rule is written only for other directives, for example auth-zone: The authority zone with the name closest to the name looked up is used.) | verified | `local-zone-root-deny-keeps-builtin-zones` |
| A | 5.3 | ipset passes the grammar but fails at zone registration in builds without ipset support | util/configparser.y server_local_zone includes "ipset" in its accepted list. The branch that stores an ipset zone is inside #ifdef USE_IPSET, so in builds without it the zone goes into the normal cfg->local_zones, where local_zone_str2type("ipset") in services/localzone.c lz_enter_zone() returns 0 and registration fails | none (the word ipset does not appear once in `unbound.conf.5.in`; a full-text search for 'ipset' finds nothing) | verified | `local-zone-type-ipset-accepted-then-fails` |
| A | 5.3 | truncate is a localzone_type but local-zone: does not accept it | services/localzone.c local_zone_str2type() accepts "truncate", but it is not in the accepted list of util/configparser.y server_local_zone, so it is a yyerror | none (the word truncate does not appear once in `unbound.conf.5.in`; a full-text search for 'truncate' finds nothing) | verified | `local-zone-type-truncate-rejected-in-conf` |
| A | 5.5 | An IPv6 zone ID (%eth0) is rejected in AAAA RDATA | sldns/str2wire.c sldns_str2wire_aaaa_buf() just calls inet_pton(AF_INET6, str, ...) and does not handle %. util/net_help.c ipstrtoaddr(), by contrast, splits off the % and turns it into sin6_scope_id with if_nametoindex() | none (neither the local-data entry nor anything about AAAA mentions zone IDs; searched for '%', 'scope' and 'zone id') | verified | `local-data-aaaa-rejects-ipv6-zone-id` |
| A | 5.5 | Omitting the local-data TTL gives 3600 | services/localzone.c rrstr_get_rr_content() and get_rr_nameclass() pass the default TTL as a literal: sldns_str2wire_rr_buf(..., 3600, ...). It cannot be changed from the configuration | none (the local-data entry in `unbound.conf.5.in` does not give the default TTL; searched for 'local-data', 'ttl' and 'default') | verified | `local-data-default-ttl-3600` |
| A | 5.5 | local-data owner names are not distinguished by a trailing dot either, and an identical RR is ignored as a duplicate | sldns/str2wire.c sldns_str2wire_dname_buf_rel() adds the root label to a name without a trailing dot, so the wire names are identical (services/localzone.c rrstr_get_rr_content() passes a NULL origin, so the name is not treated as relative). With the same name and type, services/localzone.c local_zone_enter_rr() reuses the same RRset, and because the rdata is also the same rr_is_duplicate() is true, so it logs verbose("ignoring duplicate RR") and does not add it | none (the local-data: entry does not say whether a trailing dot is required) | verified | `local-data-owner-trailing-dot-is-same-name` |
| A | 5.5 | local-data-ptr is rewritten into a PTR local-data string at config parse time | util/configparser.y server_local_data_ptr puts the result of cfg_ptr_reverse() into cfg->local_data. util/config_file.c cfg_ptr_reverse() turns "<IP> [words in between] <name>" into "<reverse name>. [words in between] PTR <name>". From there the path is exactly the same as local-data | doc/unbound.conf.5.in, local-data-ptr entry: Configure local data shorthand for a PTR record with the reversed IPv4 or IPv6 address and the host name. | verified | `local-data-ptr-is-rewritten-to-local-data` |
| A | 5.5 | local-data with the same name and type are merged into one RRset, in the order written | services/localzone.c local_zone_enter_rr() reuses the existing RRset through local_data_find_type() and adds to it with rrset_insert_rr(). cfg->local_data is built by head insertion, so it is reversed, and rrset_insert_rr inserts at index 0, so the two reversals restore the written order | none (nothing on what happens when several local-data share a name and type) | verified | `local-data-same-name-type-merges-rrset` |
| A | 5.5 | A TXT character-string of up to 255 wire bytes is accepted | sldns/str2wire.c sldns_str2wire_str_buf(). It returns LDNS_WIREPARSE_ERR_INVALID_STR when sl >= 255, so at most 255 bytes can be written | none (the local-data entry in `unbound.conf.5.in` gives no length limit for a character-string; searched for '255', 'character-string' and 'length') | verified | `local-data-txt-255-bytes-ok` |
| A | 5.5 | A TXT character-string of 256 bytes is a configuration error | sldns/str2wire.c sldns_str2wire_str_buf(). The sl >= 255 check at the top of the loop returns LDNS_WIREPARSE_ERR_INVALID_STR before the 256th byte is written | none (the local-data entry gives no length limit) | verified | `local-data-txt-256-bytes-rejected` |
| A | 5.5 | The TXT limit of 255 counts the wire length after escape resolution, not the input length | sldns/str2wire.c sldns_str2wire_str_buf() counts sl after sldns_parse_char() has resolved each character. sldns/parseutil.c sldns_parse_escape() folds \DDD into one byte | none (the local-data entry gives neither the length limit nor how escapes are counted) | verified | `local-data-txt-limit-is-wire-length` |
| A | 5.5 | local-data containing * matches only queries for the literal name * | sldns/str2wire.c sldns_str2wire_dname_buf_rel() does not treat * specially and writes it as a one-character label. The local_data tree lookup in services/localzone.c, local_zone_find_data() / rbtree_search(), is an exact match, so only a name with the same stored * label matches | none (`unbound.conf.5.in` does not say how local-data containing a wildcard is stored; it only advises using a stub-zone for wildcards) | verified | `local-data-wildcard-label-matches-only-literally` |
| A | 5.6 | nodefault on a name that is not a built-in zone only warns, and checkconf succeeds | smallapp/unbound-checkconf.c nodefaultzonescheck() compares the name against as112_zones / local_zones_default_special / local_zones_default_reverse / localhost., and if it is in none of them it only does fprintf(stderr, ... warning ...) without making it fatal | none (the man pages do not list what `unbound-checkconf` validates and what it does not) | verified | `checkconf-warns-nodefault-without-builtin-zone` |
| A | 5.6 | A forward-zone for a private reverse zone is never used: the built-in AS112 zone answers first | util/as112.c as112_zone_array contains 168.192.in-addr.arpa., and services/localzone.c local_zone_enter_defaults() adds it as a static zone. daemon/worker.c worker_handle_request() calls local_zones_answer() before the query reaches the iterator, so the static zone answers NXDOMAIN and the forward-zone is never looked up | doc/unbound.conf.5.in, unblock-lan-zones entry: If enabled, then for private address space, the reverse lookups are no longer filtered. (nothing on a forward-zone or stub-zone written for such a zone being overridden) | verified | `forward-zone-private-reverse-shadowed-by-builtin` |
| A | 5.6 | With no configuration at all, test. answers as a built-in static zone | services/localzone.c default_zones_special_array lists test. invalid. onion. home.arpa. resolver.arpa. service.arpa., and local_zone_enter_defaults() adds a static zone with SOA / NS for each through add_empty_default() | doc/unbound.conf.5.in L3199-: The default zones are localhost, reverse 127.0.0.1 and ::1, the home.arpa, resolver.arpa, service.arpa, onion, test, invalid and the AS112 zones. | verified | `local-zone-builtin-test-zone-answers-locally` |
| A | 5.6 | nodefault removes the built-in zone, and queries go upstream | util/configparser.y server_local_zone puts nodefault into cfg->local_zones_nodefault rather than cfg->local_zones. services/localzone.c add_empty_default() skips the built-in contents when lz_nodefault() matches | doc/unbound.conf.5.in, nodefault entry: Used to turn off default contents for AS112 zones. The other types also turn off default contents for the zone. | verified | `local-zone-nodefault-removes-builtin-zone` |
| A | 6.4 | unbound-checkconf validates the forward-addr address string itself | smallapp/unbound-checkconf.c checkconf() calls check_fwd() and check_hints() after morechecks(), and they run the same forwards_apply_cfg() / hints_apply_cfg() as the daemon. The actual failure is authextstrtoaddr() in iterator/iter_fwd.c read_fwds_addr() | none (`unbound-checkconf.8.in` only says it checks the configuration file and does not list what is validated) | verified | `checkconf-validates-forward-addr` |
| A | 6.4 | forward-addr accepts an IPv6 zone ID and turns it into sin6_scope_id | util/net_help.c ipstrtoaddr() splits off the %, stores if_nametoindex(s+1) in sa->sin6_scope_id and then calls inet_pton(). sldns/str2wire.c sldns_str2wire_aaaa_buf() has no such handling | none (the forward-addr entry does not mention zone IDs) | verified | `forward-addr-accepts-ipv6-zone-id` |
| A | 6.4 | There is no limit on the number of forward-addr entries | iterator/iter_fwd.c read_fwds_addr() just walks cfg->forwards->addrs without counting. iterator/iter_delegpt.c delegpt_add_addr_mlc() has no limit on the count either | none (the forward-addr entry says nothing about the number of entries) | verified | `forward-addr-many-entries-no-limit` |
| A | 6.4 | forward-addr @-1 is not an error and becomes 65535 | util/net_help.c authextstrtoaddr(). atoi("-1") is -1, not 0, so it slips past the port == 0 check, and the (uint16_t)(-1) in ipstrtoaddr() makes it 65535 | none (the forward-addr entry says nothing about the valid range) | verified | `forward-addr-port-negative` |
| A | 6.4 | A forward-addr @port that does not start with a digit is rejected | util/net_help.c authextstrtoaddr(). atoi("dns") is 0 and "dns" is not the string "0", so it returns 0. iterator/iter_fwd.c read_fwds_addr() then logs cannot parse forward ... ip address, and smallapp/unbound-checkconf.c check_fwd() fails | none (the forward-addr entry says the port is a number but not what happens otherwise) | verified | `forward-addr-port-non-numeric-rejected` |
| A | 6.4 | The forward-addr @port is not range-checked and is truncated to uint16 | util/net_help.c authextstrtoaddr() does port = atoi(s+1) without a range check, and util/net_help.c ipstrtoaddr() truncates it with p = (uint16_t)port | none (the forward-addr entry in `unbound.conf.5.in` only says To use a non-default port for DNS communication append '@' with the port number. and gives no valid range) | verified | `forward-addr-port-overflow` |
| A | 6.4 | Trailing characters after the forward-addr @port number are silently ignored | util/net_help.c authextstrtoaddr() parses the port with atoi(s+1), which stops at the first non-digit and reports no error. Only a result of 0 is checked, so 5408abc becomes 5408 | none (the forward-addr entry says nothing about what may follow the port number) | verified | `forward-addr-port-trailing-garbage-ignored` |
| A | 6.4 | forward-addr @0 is accepted as port 0 | util/net_help.c authextstrtoaddr(). port = atoi(s+1) is 0, and the only check for 0 is strcmp(s+1,"0")!=0, which "0" passes, so ipstrtoaddr() is called with port 0 | none (the forward-addr entry says nothing about the valid range) | verified | `forward-addr-port-zero-accepted` |
| A | 6.4 | forward-tls-upstream: yes still connects to port 53 unless @853 or #name is written | util/net_help.c authextstrtoaddr() starts from port = UNBOUND_DNS_PORT (53) and changes it only for @port or for # (UNBOUND_DNS_OVER_TLS_PORT). forward-tls-upstream sets dp->ssl_upstream in iterator/iter_fwd.c read_forwards() and is never consulted when the address is parsed. daemon/daemon.c always creates the outgoing TLS context, so the TLS connection is attempted | doc/unbound.conf.5.in, forward-tls-upstream entry: Enabled or disable whether the queries to this forwarder use TLS for transport. (nothing about the port) | verified | `forward-tls-upstream-keeps-port-53` |
| A | 6.4 | stub-addr goes through the same authextstrtoaddr() as forward-addr, so its port is truncated the same way | iterator/iter_hints.c read_stubs_addr() and iterator/iter_fwd.c read_fwds_addr() both call util/net_help.c authextstrtoaddr(). On the config side, util/configparser.y also does cfg_strlist_insert() into the same struct config_stub | doc/unbound.conf.5.in, stub-addr entry: To use a non-default port for DNS communication append '@' with the port number. (the same text as the forward-addr entry; neither gives the valid range) | verified | `stub-addr-shares-port-parsing-with-forward-addr` |
| A | 6.5 | forward-first does not change the destination, only the fallback behaviour | iterator/iter_fwd.c read_forwards() only uses isfirst for dp->has_parent_side_NS = !s->isfirst, which does not affect the destinations built by read_fwds_addr() | doc/unbound.conf.5.in, forward-first entry: If a forwarded query is met with a SERVFAIL error and this option is enabled Unbound will fall back to less specific resolution. | verified | `forward-first-does-not-change-destination` |
| A | 6.5 | A forward-zone and a stub-zone with the same name: the forward-zone silently wins, wherever it is written | iterator/iterator.c processInitRequest() tries forward_request() (L1593) before the stub lookup in step 2 (L1626 onwards), and forwards_lookup() finds the forward zone. iterator/iter_fwd.c make_stub_holes() would punch a hole in the forward tree for a stub-zone, but skips it with the comment Already a forward zone there. and logs nothing | none (neither the stub-zone nor the forward-zone section says which one is used when both have the same name) | verified | `forward-zone-beats-stub-zone-same-name` |
| A | 6.5 | With two forward-zone clauses for the same name, the one written last wins | util/configparser.y forwardstart inserts each forward-zone clause at the head of cfg->forwards, so iterator/iter_fwd.c read_forwards() walks them in reverse order. forwards_insert_data() logs duplicate forward zone %s ignored. when rbtree_insert() fails and frees the new one, so the clause processed first, which is the one written last, remains | none (the forward-zone section says clauses may be repeated for different names, but not what happens when a name is repeated) | verified | `forward-zone-duplicate-last-wins` |
| B | 5.3 | local-data without a local-zone creates one transparent zone at the shortest common parent domain | services/localzone.c lz_setup_implicit(). It walks the names of local-data not covered by any zone, finds the minimum number of matching labels with dname_lab_cmp(), creates exactly one local_zone_transparent zone there, and logs it with log_nametypeclass(VERB_ALGO, "implicit transparent local-zone", ...) | doc/unbound.conf.5.in, local-data entry: If local-data is configured that is not a subdomain of a local-zone, a transparent local-zone is configured. | verified | `local-zone-implicit-transparent-at-shared-suffix` |
| B | 5.3 | local-data with no common parent domain puts the implicit transparent zone at the root | services/localzone.c lz_setup_implicit(). When the matching label count match is 0, dname_remove_labels() removes every label and one local_zone_transparent zone is created at the root | doc/unbound.conf.5.in, local-data entry: If local-data is configured that is not a subdomain of a local-zone, a transparent local-zone is configured. | verified | `local-zone-implicit-transparent-can-land-at-root` |
| B | 5.3 | local-zone-override accepts truncate, which local-zone: rejects | util/configparser.y server_local_zone_override does not validate the type and only does cfg_str3list_insert(). services/localzone.c lz_enter_override() uses local_zone_str2type(), so truncate and nodefault, which are not in the list local-zone: accepts, also pass | doc/unbound.conf.5.in L3486-: Use this localzone type, regardless the type configured for the local zone ... (nothing on which types can be used) | verified | `local-zone-override-accepts-truncate` |
| B | 5.3 | local-zone-override is a fatal error unless a local-zone with the same name already exists | services/localzone.c lz_enter_override() logs log_err("no local-zone for local-zone-override %s") and returns 0 when local_zones_find() (an exact-match lookup) finds nothing. lz_enter_overrides() stops on 0 | doc/unbound.conf.5.in L3486-: Override the local zone type for queries from addresses matching netblock. Use this localzone type, regardless the type configured for the local zone (both tagged and untagged) and regardless the type configured using access-control-tag-action. | verified | `local-zone-override-requires-exact-zone` |
| B | 5.3 | always_nodata works but is missing from the local-zone type list | util/configparser.y server_local_zone accepts "always_nodata", and services/localzone.c local_zone_str2type() converts it to local_zone_always_nodata | doc/unbound.conf.5.in L2910-L2925, the list of types under local-zone: deny, refuse, static, transparent, redirect, nodefault, typetransparent, inform, inform_deny, inform_redirect, always_transparent, block_a, block_aaaa, block_a_wdata, block_aaaa_wdata, always_refuse, always_nxdomain, always_null, noview (always_nodata and always_deny are not in it). There are, however, separate entries for .B always_nodata at L3156 and .B always_deny at L3162 | verified | `local-zone-type-always-nodata-works` |
| B | 5.4 | A wildcard CNAME target in a redirect zone is expanded only for non-CNAME queries; a CNAME query gets the literal * | services/localzone.c local_data_answer(). The branch that builds the alias and expands a wildcard target with dname_is_wild() runs only when qinfo->qtype != LDNS_RR_TYPE_CNAME. A CNAME query falls through to the generic redirect branch, which only renames the owner to the query name and encodes the stored rdata unchanged | doc/unbound.conf.5.in, redirect entry: As different from CNAME records that are used elsewhere, in the redirect type local-zone, it is supported that in the target of the record a wildcard label gets expanded to the query name, with for example: example.com. CNAME *.foo.net. gets expanded to www.example.com. CNAME www.example.com.foo.net.. | verified | `local-zone-redirect-cname-wildcard-not-expanded-for-cname-query` |
| B | 5.4 | In a redirect zone, a CNAME cannot coexist with local-data of other types | services/localzone.c local_zone_enter_rr(). In redirect / inform_redirect, when node->rrsets already exists and either the new record or the existing one is a CNAME, it does log_err and returns 0 | doc/unbound.conf.5.in, `redirect` entry: The CNAME record has to be with the zone name of the local-zone, and there can be one CNAME, not more. | verified | `local-zone-redirect-cname-with-other-data` |
| B | 6.4 | With #name the default port is the compile-time constant 853, not the tls-port: setting | util/net_help.c authextstrtoaddr() assigns UNBOUND_DNS_OVER_TLS_PORT (853 in config.h.in) directly. cfg->ssl_port (tls-port:) is read only on the listening side in services/listen_dnsport.c and in services/authzone.c, and plays no part in parsing forward-addr / stub-addr | doc/unbound.conf.5.in, forward-addr entry: If only '#' is used the default port is the configured tls-port. | verified | `forward-addr-name-ignores-tls-port-setting` |
| B | 6.4 | Adding #name alone changes the destination port to 853, and TLS is never consulted | util/net_help.c authextstrtoaddr(). On the path with # and no @ it unconditionally assigns port = UNBOUND_DNS_OVER_TLS_PORT (853). Neither cfg->ssl_upstream nor dp->ssl_upstream, the equivalents of forward-tls-upstream, is looked at here | doc/unbound.conf.5.in, forward-addr entry: If TLS is enabled, then you can append a '#' and a name, then it'll check the tls authentication certificates with that name. / If only '#' is used the default port is the configured tls-port. | verified | `forward-addr-name-implies-port-853` |
| C | 5.2 | With single quotes the inner double quotes stay in the value, so the TXT is one character-string | util/configlexer.lex SQANY = [^\'\n\r\\]\|\\. so " is an ordinary character. sldns/str2wire.c rrinternal_get_quoted() sees the leading " and switches the delimiter to " | doc/unbound.conf.5.in, local-data entry: For record types such as TXT, use single quotes, as in: local-data: 'example. TXT "text"' | verified | `conf-lexer-single-quote-keeps-double-quote` |
| C | 5.2 | An escaped \" inside double quotes does not become a TXT quote, and breaks the value and the word boundaries | util/configlexer.lex leaves \\" as two characters. sldns/str2wire.c rrinternal_get_quoted() sees a leading \\, so it does not treat the text as quoted, and the delimiters stay "\n\t ". sldns/parseutil.c sldns_parse_char() later resolves \\" to ", so the quote ends up in the value | doc/unbound.conf.5.in, local-data entry: For record types such as TXT, use single quotes, as in: local-data: 'example. TXT "text"' | verified | `local-data-double-quote-escape-splits-txt` |
| C | 5.3 | block_aaaa returns NODATA for AAAA even when local-data exists | The condition at services/localzone.c local_zones_answer() L1990 includes lzt != local_zone_block_aaaa, so local_data_answer() is not reached. local_zones_zone_answer() L1761 returns NOERROR/NODATA for qtype == AAAA | doc/unbound.conf.5.in L3115, the .B block_aaaa entry | verified | `local-zone-block-aaaa-ignores-local-data` |
| C | 5.3 | block_aaaa_wdata returns local-data when there is any | block_aaaa_wdata is not in the exclusion condition at services/localzone.c local_zones_answer() L1990, so local_data_answer() takes effect first. Without local-data, local_zones_zone_answer() L1761 returns NODATA | doc/unbound.conf.5.in L3133, the .B block_aaaa_wdata entry | verified | `local-zone-block-aaaa-wdata-uses-local-data` |
| C | 5.4 | The CNAME target of a redirect zone is resolved upstream even when a local-zone covers it | services/localzone.c local_data_answer() stores the CNAME in qinfo->local_alias and returns. daemon/worker.c worker_handle_request() then replaces the qname with the alias target via local_alias_shallow_copy_qname() and goes on to the cache and the mesh. local_zones_answer() has already run for the original name and is not called again, and the iterator does not consult local zones | doc/unbound.conf.5.in, redirect entry: The resolution proceeds with upstream DNS resolution, and that does not include the lookup in local zones. So the record is not able to point in local zones, but it can point to upstream DNS answers. | verified | `local-zone-redirect-cname-target-skips-local-zones` |
| C | 5.4 | In a redirect zone, local-data that does not exactly match the apex is a fatal error | services/localzone.c lz_enter_rr_into_zone(). When z->type is local_zone_redirect / local_zone_inform_redirect and query_dname_compare(z->name, nm) != 0, it does log_err and returns 0 | doc/unbound.conf.5.in, `redirect` entry: There may be no local data beneath the zone name. | verified | `local-zone-redirect-non-apex-data` |
| C | 5.5 | In a static zone, an A query for a name with only a CNAME gets the CNAME alone, even when its target is in the same zone | services/localzone.c local_data_answer() calls local_data_find_type(ld, qtype, 1). With alias_ok set it returns the CNAME RRset when there is no RRset of the asked type. Outside redirect / inform_redirect the answer is encoded from that one RRset by local_encode(), and nothing looks up the target | doc/unbound.conf.5.in, redirect entry: CNAME resolution is supported only in type redirect local-zone, and in type inform_redirect local-zone. | verified | `local-data-cname-in-static-zone-not-chased` |
| C | 5.5 | local-data wildcards are not expanded; the name is a literal that contains * | services/localzone.c has no wildcard matching. The only use of dname_is_wild() is CNAME target expansion in local_zone_answer_cname() (L1593), not owner name matching. The comment at local_zones_zone_answer() L1728 also says no additional section processing, cname, dname or wildcard processing. The name is converted to wire form unchanged by sldns_str2wire_dname(), and * is stored as a one-byte label | doc/unbound.conf.5.in L2935-L2938 and L3455-L3458: If you need more complicated authoritative data, with referrals, wildcards, CNAME/DNAME support, or DNSSEC authoritative service, setup a stub-zone for it as detailed in the stub zone section below. | verified | `local-data-wildcard-label-is-literal-name` |
| undecided | 6.5 | With forward-no-cache: yes, does a repeated identical query also go upstream? | iterator/iter_fwd.c read_forwards() sets dp->no_cache = s->no_cache. It plays no part in choosing the destination. To see from outside whether the cache is used, the upstream must first return an answer that can enter the cache | doc/unbound.conf.5.in, forward-no-cache entry: If enabled, data inside the forward is not cached. This is useful when you want immediate changes to be visible. | inconclusive | `forward-no-cache-repeat-query-hits-upstream` |
