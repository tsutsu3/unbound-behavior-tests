<!-- generated by gen.py. DO NOT EDIT.
     unbound: 1.26.0 (release-1.26.0, a45da353)
     cases: d3ac0e6 -->


# 付録: 実装から確認した挙動 (Unbound 1.26.0)

分類は man についての主張である。A = man に記述なし、B = 記述があるが実装と食い違う、C = 記述と実装は一致するが結果が直感に反する、undecided = 判断保留。

| 分類 | 節 | 挙動 | 根拠 (source) | man | 状態 | ケース |
| --- | --- | --- | --- | --- | --- | --- |
| A | 5.2 | include: のワイルドカードの対象ディレクトリが無いと、パターンそのものをファイル名としたエラーになる | util/configlexer.lex config_start_include_glob() は glob() に GLOB_ERR を渡すので、開けないディレクトリがあると GLOB_NOMATCH 以外のエラーで打ち切られる。その分岐は展開前のパターンを config_start_include() に渡し（let original deal with it）、conf.d/*.conf という名前のファイルを開こうとして失敗する | doc/unbound.conf.5.in INCLUDING FILES: Wildcards can be used to include multiple files, see glob(7).（ディレクトリが無いパターンの扱いの記述がない） | verified | `conf-include-glob-missing-directory-is-error` |
| A | 5.2 | include: のワイルドカードは、ディレクトリがあれば 1 ファイルにも一致しなくても黙って無視される | util/configlexer.lex config_start_include_glob() は、名前にワイルドカード文字（* ? [ { ~）があれば GLOB_ERR を付けて glob() を呼ぶ。ディレクトリがあって何も一致しなければ glob() は GLOB_NOMATCH を返し、関数は何も言わずに戻る。ワイルドカードを含まない名前は config_start_include() に直接渡り、開けなければエラーになる | doc/unbound.conf.5.in INCLUDING FILES: Wildcards can be used to include multiple files, see glob(7).（何にも一致しないパターンの扱いの記述がない） | verified | `conf-include-glob-no-match-is-silent` |
| A | 5.2 | include: のワイルドカードはファイル名のソート順で読まれ、最後のファイルが勝つ | util/configlexer.lex config_start_include_glob() は GLOB_NOSORT を立てない（do not set GLOB_NOSORT so the results are sorted and in a predictable order というコメントがある）。そのうえで for(i=(int)g.gl_pathc-1; i>=0; i--) と逆順にインクルードスタックへ積むので、取り出す順序はソート順になる。あとは cfg_str2list_insert() の先頭挿入で最後に読まれた行が勝つ | doc/unbound.conf.5.in INCLUDING FILES: Wildcards can be used to include multiple files, see glob(7).（読み込み順については記述がない） | verified | `conf-include-glob-processed-in-sorted-order` |
| A | 5.2 | include: した設定は展開位置に置かれ、その後に書いた行が勝つ | util/configlexer.lex config_start_include() が yy_switch_to_buffer() で入力バッファを差し替え、EOF で config_end_include() が戻すので、トークン列としては include: の位置にそのまま展開される。そのうえで util/config_file.c cfg_str2list_insert() が先頭挿入なので、後に読まれた行が cfg->local_zones の先頭に来て、services/localzone.c lz_enter_zones() が先に登録して勝つ | doc/unbound.conf.5.in INCLUDING FILES: Processing continues as if the text from the included file was copied into the config file at that point. | verified | `conf-include-later-line-wins` |
| A | 5.2 | include: を後ろに置くと、インクルードした側が本体の設定に勝つ | util/configlexer.lex の展開はテキスト位置そのものなので、本体か include 先かという区別は残らない。util/config_file.c cfg_str2list_insert() の先頭挿入と services/localzone.c lz_enter_zone_dname() の重複時「既存を返す」の組み合わせで、ファイル内での位置だけが勝敗を決める | doc/unbound.conf.5.in INCLUDING FILES: It can appear anywhere, it accepts a single file name as argument. Processing continues as if the text from the included file was copied into the config file at that point. | verified | `conf-include-position-not-file-decides` |
| A | 5.2 | include-toplevel: の直後は節を開き直さないと構文エラーになる | util/configlexer.lex の include-toplevel 規則が config_start_include_glob(yytext, 1) のあと VAR_FORCE_TOPLEVEL を返し、インクルード先の EOF でも prev_toplevel なら同じトークンを返す。util/configparser.y force_toplevel が cfg_parser->started_toplevel = 0 にするため、直後のディレクティブは節の外になる。エラー文言は util/config_file.c ub_c_error() が started_toplevel == 0 のときに差し替える専用のもの | doc/unbound.conf.5.in INCLUDING FILES: For a more structural include option, the include-toplevel: directive can be used. This closes whatever section clause is currently active (if any) and forces the use of section clauses in the included files and right after this directive. | verified | `conf-include-toplevel-closes-section` |
| A | 5.2 | レキサはバックスラッシュエスケープを解かず後段の RR パーサへ渡す | util/configlexer.lex。DQANY/SQANY/UNQUOTEDLETTER は \\. を 2 文字としてまとめて受理するだけで、yytext をそのまま strdup する。解決は sldns/parseutil.c sldns_parse_char() 側 | none（FILE FORMAT 節にバックスラッシュの記述がない。'backslash' 'escape' で検索） | verified | `conf-lexer-backslash-not-resolved` |
| A | 5.2 | 二重引用符の中の二重引用符はそこで値を終わらせる | util/configlexer.lex `<quotedstring>\"` 規則。DQANY = [^\"\n\r\\]\|\\. なので、バックスラッシュを伴わない " は文字列を閉じる | none（`unbound.conf.5.in` の FILE FORMAT 節は空白・コメント・コロンだけを述べ、引用符の扱いを一切書いていない） | verified | `conf-lexer-double-quote-terminates-early` |
| A | 5.2 | local-zone の第 2 引数を書き忘れると次の行のディレクティブ名が引数として食われる | util/configlexer.lex。YDVAR(2, VAR_LOCAL_ZONE) が num_args=2 で val 状態に入り、改行は <INITIAL,val>{NEWLINE} で読み飛ばされる。UNQUOTEDLETTER はコロンを含むため、次行の `local-data:` が <val>{UNQUOTEDLETTER}* に一致して 2 個目の STRING_ARG になる | none（FILE FORMAT 節に引数不足時の扱いの記述がない） | verified | `conf-lexer-missing-argument-eats-next-directive` |
| A | 5.2 | 引用符で囲んだ値は行をまたげない（行継続の構文がない） | util/configlexer.lex `<quotedstring>{NEWLINE}` 規則が yyerror("newline inside quoted string, no end \"") を出して INITIAL へ戻る。DQANY も NEWLINE を含まない | none（FILE FORMAT 節に行継続の記述も、引用値が行内に収まる必要があるという記述もない） | verified | `conf-lexer-no-line-continuation` |
| A | 5.2 | local-data の値の中では # ではなく ; がコメント開始になる | sldns/parse.c sldns_bget_token_par()。c == ';' かつ quoted でなく直前が \\ でなければ com=1 になり、以降は行末まで空白に置き換えられる。設定ファイル側の # コメントは util/configlexer.lex の {SPACE}*{COMMENT}.* で、引用符の内側には適用されない | none（man の FILE FORMAT は # だけをコメントとして説明し、local-data の値が RR テキストとして別のコメント規則を持つことに触れていない） | verified | `local-data-semicolon-starts-rr-comment` |
| A | 5.3 | 重複時に捨てられるのは local-zone 行だけで local-data は 1 つのゾーンに集まる | services/localzone.c lz_enter_zones() は重複ゾーンを捨てるが、local-data は lz_enter_data() が cfg->local_data を走査して lz_enter_rr_str() で覆うゾーンを検索して入れるため、どちらの記述ブロックのものも残る | none（重複 local-zone に関する記述が man にない） | verified | `local-zone-duplicate-keeps-both-local-data` |
| A | 5.3 | 同名 local-zone を重複させると設定ファイルで後に書いた type が残る | util/config_file.c cfg_str2list_insert() が先頭挿入なので cfg->local_zones は記述と逆順。services/localzone.c lz_enter_zones() が先頭から登録し、lz_enter_zone_dname() は重複時に log_warn して既存ゾーンを返すため、先に登録された = 最後に書かれた行が残る | none（`unbound.conf.5.in` に local-zone の重複時の扱いも、同一ディレクティブ複数回の格納順も記述がない。'duplicate' 'twice' 'last one' で検索） | verified | `local-zone-duplicate-last-line-wins` |
| A | 5.3 | 暗黙の transparent ゾーンでは同名別型の問い合わせが NODATA になる | services/localzone.c lz_setup_implicit() が local_zone_transparent のゾーンを作る。transparent は同名で型が無い場合 NOERROR/NODATA を返す | doc/unbound.conf.5.in transparent の項: If the query is for a name given in local-data but no such type of data is given in localdata, then a NOERROR NODATA answer is returned. | verified | `local-zone-implicit-transparent-gives-nodata` |
| A | 5.3 | local-zone 名は末尾ドットの有無と大文字小文字を区別せず、同じゾーンとして重複扱いになる | services/localzone.c parse_dname() → sldns/str2wire.c sldns_str2wire_dname_buf_rel()。末尾が . でなければ /* add root label if last char was not '.' */ の分岐でルートラベルを付けるので、example.com と example.com. は同一のワイヤ名になる。ゾーン木のキー比較 local_zone_cmp() は dname_lab_cmp() で大文字小文字を区別しない。したがって rbtree_insert() が重複を検出し、lz_enter_zone_dname() が log_warn("duplicate local-zone %s") を出して既存ゾーンを返す | none（`unbound.conf.5.in` の local-zone: の項に、ゾーン名の末尾ドットの要否も大文字小文字の扱いも記述がない） | verified | `local-zone-name-trailing-dot-and-case-are-same-zone` |
| A | 5.3 | ルートに deny を置いても、より具体的なゾーンの配下には効かず組み込みゾーンは応答し続ける | services/localzone.c local_zones_tags_lookup() が rbtree_find_less_equal() と親ゾーンへの遡りで、問い合わせ名を覆う最も近いゾーンを選ぶ。local_zone_enter_defaults() は lz_exists() が同名ゾーンを見つけない限り組み込みゾーンを入れ、ルートのゾーンは同名にならないので test. は . の下に残る | none（local-zone: の項に、ゾーンが入れ子のときどれが答えるかの記述がない。最も近いものを使うという規則は auth-zone など別の項にだけある: The authority zone with the name closest to the name looked up is used.） | verified | `local-zone-root-deny-keeps-builtin-zones` |
| A | 5.3 | ipset は文法上は受理されるが ipset 非対応ビルドではゾーン登録で失敗する | util/configparser.y server_local_zone は "ipset" を受理リストに含める。ipset を格納する分岐は #ifdef USE_IPSET の中なので、非対応ビルドでは通常の cfg->local_zones に入り、services/localzone.c lz_enter_zone() の local_zone_str2type("ipset") が 0 を返して失敗する | none（`unbound.conf.5.in` に ipset という語が 1 度も現れない。'ipset' で全文検索して 0 件） | verified | `local-zone-type-ipset-accepted-then-fails` |
| A | 5.3 | truncate は localzone_type だが local-zone: では受理されない | services/localzone.c local_zone_str2type() は "truncate" を受理するが、util/configparser.y server_local_zone の受理リストに truncate がないため yyerror になる | none（`unbound.conf.5.in` に truncate という語が 1 度も現れない。'truncate' で全文検索して 0 件） | verified | `local-zone-type-truncate-rejected-in-conf` |
| A | 5.5 | AAAA の RDATA では IPv6 zone ID（%eth0）が拒否される | sldns/str2wire.c sldns_str2wire_aaaa_buf() は inet_pton(AF_INET6, str, ...) をそのまま呼ぶだけで % を扱わない。util/net_help.c ipstrtoaddr() は逆に % を切り出して if_nametoindex() で sin6_scope_id にする | none（local-data の項にも AAAA の項にも zone ID の記述がない。'%' 'scope' 'zone id' で検索） | verified | `local-data-aaaa-rejects-ipv6-zone-id` |
| A | 5.5 | local-data の TTL を省略すると 3600 になる | services/localzone.c rrstr_get_rr_content() および get_rr_nameclass() が sldns_str2wire_rr_buf(..., 3600, ...) と既定 TTL をリテラルで渡す。設定項目からは変更できない | none（`unbound.conf.5.in` の local-data の項に TTL 省略時の既定値の記述がない。'local-data' 'ttl' 'default' で検索） | verified | `local-data-default-ttl-3600` |
| A | 5.5 | local-data のオーナー名も末尾ドットの有無で区別されず、同じ RR は重複として無視される | sldns/str2wire.c sldns_str2wire_dname_buf_rel() が末尾ドット無しの名前にルートラベルを補うので同一のワイヤ名になる（origin は services/localzone.c rrstr_get_rr_content() が NULL を渡すため相対名として扱われない）。同一名・同一型なので services/localzone.c local_zone_enter_rr() が同じ RRset を再利用し、rdata も同じなので rr_is_duplicate() が真になって verbose("ignoring duplicate RR") のうえ追加されない | none（local-data: の項に末尾ドットの要否の記述がない） | verified | `local-data-owner-trailing-dot-is-same-name` |
| A | 5.5 | local-data-ptr は設定パース時に PTR の local-data 文字列へ書き換えられる | util/configparser.y server_local_data_ptr が cfg_ptr_reverse() の結果を cfg->local_data に入れる。util/config_file.c cfg_ptr_reverse() が "<IP> [間の語] <name>" を "<逆引き名>. [間の語] PTR <name>" に組み立てる。以降の経路は local-data と完全に同一 | doc/unbound.conf.5.in local-data-ptr の項: Configure local data shorthand for a PTR record with the reversed IPv4 or IPv6 address and the host name. | verified | `local-data-ptr-is-rewritten-to-local-data` |
| A | 5.5 | 同名同型の local-data は 1 つの RRset にまとめられ、記述順で並ぶ | services/localzone.c local_zone_enter_rr() が local_data_find_type() で既存 RRset を再利用し rrset_insert_rr() で追加する。cfg->local_data は先頭挿入で逆順、rrset_insert_rr は添字 0 に挿入するので、2 度の反転で記述順に戻る | none（同名同型の local-data を複数書いたときの扱いの記述がない） | verified | `local-data-same-name-type-merges-rrset` |
| A | 5.5 | TXT の character-string はワイヤ長 255 バイトまで受理される | sldns/str2wire.c sldns_str2wire_str_buf()。sl >= 255 で LDNS_WIREPARSE_ERR_INVALID_STR を返すため、書き込めるのは 255 バイトまで | none（`unbound.conf.5.in` の local-data の項に character-string の長さ上限の記述がない。'255' 'character-string' 'length' で検索） | verified | `local-data-txt-255-bytes-ok` |
| A | 5.5 | TXT の character-string が 256 バイトになると設定エラーになる | sldns/str2wire.c sldns_str2wire_str_buf()。ループ先頭の sl >= 255 判定で 256 バイト目の書き込み前に LDNS_WIREPARSE_ERR_INVALID_STR を返す | none（local-data の項に長さ上限の記述がない） | verified | `local-data-txt-256-bytes-rejected` |
| A | 5.5 | TXT の 255 の上限はエスケープ解決前の入力長ではなく解決後のワイヤ長で数える | sldns/str2wire.c sldns_str2wire_str_buf() は sldns_parse_char() で 1 文字ずつ解決した後の sl を数える。sldns/parseutil.c sldns_parse_escape() が \DDD を 1 バイトに畳む | none（local-data の項に長さ上限もエスケープの数え方も記述がない） | verified | `local-data-txt-limit-is-wire-length` |
| A | 5.5 | * を含む local-data は * という名前そのものへの問い合わせにだけ一致する | sldns/str2wire.c sldns_str2wire_dname_buf_rel() は * を特別扱いせず 1 文字のラベル内容として書く。services/localzone.c の local_data ツリー検索 local_zone_find_data() / rbtree_search() は完全一致なので、格納された * ラベルと同じ名前だけが一致する | none（`unbound.conf.5.in` に、ワイルドカードを書いた local-data がどう格納されるかの記述はない。ワイルドカードは stub-zone を使えという案内があるだけ） | verified | `local-data-wildcard-label-matches-only-literally` |
| A | 5.6 | 組み込みゾーンでない名前に nodefault を書くと警告だけ出て成功する | smallapp/unbound-checkconf.c nodefaultzonescheck() が as112_zones / local_zones_default_special / local_zones_default_reverse / localhost. と突き合わせ、どれにも無ければ fprintf(stderr, ... warning ...) するだけで fatal にしない | none（`unbound-checkconf` が何を検証し何を検証しないかの一覧が man にない） | verified | `checkconf-warns-nodefault-without-builtin-zone` |
| A | 5.6 | プライベートアドレスの逆引きゾーンに forward-zone を書いても、組み込みの AS112 ゾーンが先に答えて使われない | util/as112.c as112_zone_array に 168.192.in-addr.arpa. があり、services/localzone.c local_zone_enter_defaults() が static ゾーンとして入れる。daemon/worker.c worker_handle_request() は iterator に渡す前に local_zones_answer() を呼ぶので、static ゾーンが NXDOMAIN を返し、forward-zone は参照されない | doc/unbound.conf.5.in unblock-lan-zones の項: If enabled, then for private address space, the reverse lookups are no longer filtered.（その名前に書いた forward-zone や stub-zone が効かなくなることの記述はない） | verified | `forward-zone-private-reverse-shadowed-by-builtin` |
| A | 5.6 | 何も設定しなくても test. は組み込みの static ゾーンとして応答する | services/localzone.c default_zones_special_array に test. invalid. onion. home.arpa. resolver.arpa. service.arpa. が並び、local_zone_enter_defaults() が add_empty_default() で static ゾーンと SOA / NS を入れる | doc/unbound.conf.5.in L3199-: The default zones are localhost, reverse 127.0.0.1 and ::1, the home.arpa, resolver.arpa, service.arpa, onion, test, invalid and the AS112 zones. | verified | `local-zone-builtin-test-zone-answers-locally` |
| A | 5.6 | nodefault を書くと組み込みゾーンが消え、問い合わせが上流へ出る | util/configparser.y server_local_zone は nodefault を cfg->local_zones ではなく cfg->local_zones_nodefault に入れる。services/localzone.c add_empty_default() が lz_nodefault() で該当すれば組み込み内容を入れない | doc/unbound.conf.5.in nodefault の項: Used to turn off default contents for AS112 zones. The other types also turn off default contents for the zone. | verified | `local-zone-nodefault-removes-builtin-zone` |
| A | 6.4 | unbound-checkconf は forward-addr のアドレス文字列まで検証する | smallapp/unbound-checkconf.c checkconf() が morechecks() のあとに check_fwd() と check_hints() を呼び、そこで daemon と同じ forwards_apply_cfg() / hints_apply_cfg() を実行する。実際の失敗箇所は iterator/iter_fwd.c read_fwds_addr() の authextstrtoaddr() | none（`unbound-checkconf.8.in` は Checks the configuration file とだけ書き、検証範囲を列挙していない） | verified | `checkconf-validates-forward-addr` |
| A | 6.4 | forward-addr は IPv6 zone ID を受理し sin6_scope_id にする | util/net_help.c ipstrtoaddr() が % を切り出し、if_nametoindex(s+1) を sa->sin6_scope_id に入れてから inet_pton() を呼ぶ。sldns/str2wire.c sldns_str2wire_aaaa_buf() には同じ処理がない | none（forward-addr の項に zone ID の記述がない） | verified | `forward-addr-accepts-ipv6-zone-id` |
| A | 6.4 | forward-addr の件数に上限はない | iterator/iter_fwd.c read_fwds_addr() は cfg->forwards->addrs を単に走査するだけで件数を数えない。iterator/iter_delegpt.c delegpt_add_addr_mlc() にも件数の上限がない | none（forward-addr の項に件数の記述がない） | verified | `forward-addr-many-entries-no-limit` |
| A | 6.4 | forward-addr の @-1 はエラーにならず 65535 になる | util/net_help.c authextstrtoaddr()。atoi("-1") は -1 で 0 ではないため port == 0 の検査を通り抜け、ipstrtoaddr() の (uint16_t)(-1) で 65535 になる | none（forward-addr の項に値域の記述がない） | verified | `forward-addr-port-negative` |
| A | 6.4 | forward-addr の @ポートが数字で始まらないと拒否される | util/net_help.c authextstrtoaddr()。atoi("dns") は 0 で、"dns" は文字列 "0" ではないので 0 を返す。iterator/iter_fwd.c read_fwds_addr() が cannot parse forward ... ip address を出し、smallapp/unbound-checkconf.c check_fwd() が失敗する | none（forward-addr の項はポートを数値とするだけで、それ以外を書いたときの扱いの記述がない） | verified | `forward-addr-port-non-numeric-rejected` |
| A | 6.4 | forward-addr の @ポートは範囲検証されず uint16 に切り詰められる | util/net_help.c authextstrtoaddr() が port = atoi(s+1) を範囲検証せず、util/net_help.c ipstrtoaddr() が p = (uint16_t)port で切り詰める | none（`unbound.conf.5.in` forward-addr の項は To use a non-default port for DNS communication append '@' with the port number. とだけ書き、値域の記述がない） | verified | `forward-addr-port-overflow` |
| A | 6.4 | forward-addr の @ポートの数字の後ろに続く文字は黙って無視される | util/net_help.c authextstrtoaddr() は atoi(s+1) でポートを読む。atoi() は最初の数字以外の文字で止まり、エラーを返さない。検査は結果が 0 のときだけなので 5408abc は 5408 になる | none（forward-addr の項に、ポート番号の後ろに何を書けるかの記述がない） | verified | `forward-addr-port-trailing-garbage-ignored` |
| A | 6.4 | forward-addr の @0 はポート 0 として受理される | util/net_help.c authextstrtoaddr()。port = atoi(s+1) が 0 になり、0 のときの検査は strcmp(s+1,"0")!=0 だけなので、"0" はそれを通って ipstrtoaddr() がポート 0 で呼ばれる | none（forward-addr の項に値域の記述がない） | verified | `forward-addr-port-zero-accepted` |
| A | 6.4 | forward-tls-upstream: yes でも @853 か #name を書かなければ 53 番に接続する | util/net_help.c authextstrtoaddr() は port = UNBOUND_DNS_PORT (53) から始め、変えるのは @ポートか #（UNBOUND_DNS_OVER_TLS_PORT）のときだけ。forward-tls-upstream は iterator/iter_fwd.c read_forwards() で dp->ssl_upstream に入るだけで、アドレスの解釈では参照されない。daemon/daemon.c は送信用の TLS コンテキストを常に作るので、TLS 接続はそのまま試みられる | doc/unbound.conf.5.in forward-tls-upstream の項: Enabled or disable whether the queries to this forwarder use TLS for transport.（ポートについての記述がない） | verified | `forward-tls-upstream-keeps-port-53` |
| A | 6.4 | stub-addr は forward-addr と同じ authextstrtoaddr() を通るのでポートの切り詰めも同じ | iterator/iter_hints.c read_stubs_addr() と iterator/iter_fwd.c read_fwds_addr() がどちらも util/net_help.c authextstrtoaddr() を呼ぶ。設定側も util/configparser.y で同じ struct config_stub に cfg_strlist_insert() する | doc/unbound.conf.5.in stub-addr の項: To use a non-default port for DNS communication append '@' with the port number.（forward-addr の項と同文で、値域の記述はどちらにもない） | verified | `stub-addr-shares-port-parsing-with-forward-addr` |
| A | 6.5 | forward-first は宛先を変えず、フォールバック側の挙動だけを変える | iterator/iter_fwd.c read_forwards() は isfirst を dp->has_parent_side_NS = !s->isfirst に使うだけで、read_fwds_addr() が作る宛先には影響しない | doc/unbound.conf.5.in forward-first の項: If a forwarded query is met with a SERVFAIL error and this option is enabled Unbound will fall back to less specific resolution. | verified | `forward-first-does-not-change-destination` |
| A | 6.5 | 同名の forward-zone と stub-zone では、書いた位置に関係なく forward-zone が黙って勝つ | iterator/iterator.c processInitRequest() は step 2 の stub の検索（L1626 以降）より前に forward_request()（L1593）を試し、forwards_lookup() が転送ゾーンを見つける。iterator/iter_fwd.c make_stub_holes() は stub-zone の位置の転送木に穴を開けるが、同名の転送ゾーンがあると Already a forward zone there. のコメントどおり何も言わずに飛ばす | none（stub-zone 節にも forward-zone 節にも、同じ name を両方に書いたときにどちらが使われるかの記述がない） | verified | `forward-zone-beats-stub-zone-same-name` |
| A | 6.5 | 同名の forward-zone を 2 つ書くと、設定ファイルで後に書いた方が勝つ | util/configparser.y forwardstart が forward-zone 節を cfg->forwards の先頭に挿入するので、iterator/iter_fwd.c read_forwards() は記述と逆順に処理する。forwards_insert_data() は rbtree_insert() が失敗すると duplicate forward zone %s ignored. を出して新しい方を捨てるので、先に処理された = 最後に書いた節が残る | none（forward-zone 節の説明に、同じ name を 2 回書いたときの扱いの記述がない） | verified | `forward-zone-duplicate-last-wins` |
| B | 5.3 | local-zone を書かない local-data は「共通する最短の上位ドメイン」1 つに transparent ゾーンを作る | services/localzone.c lz_setup_implicit()。どのゾーンにも覆われない local-data の名前を走査し dname_lab_cmp() で一致ラベル数の最小値を求め、そこに 1 つだけ local_zone_transparent のゾーンを作り log_nametypeclass(VERB_ALGO, "implicit transparent local-zone", ...) で記録する | doc/unbound.conf.5.in local-data の項: If local-data is configured that is not a subdomain of a local-zone, a transparent local-zone is configured. | verified | `local-zone-implicit-transparent-at-shared-suffix` |
| B | 5.3 | 共通する上位ドメインが無い local-data を並べると暗黙 transparent ゾーンがルートにできる | services/localzone.c lz_setup_implicit()。一致ラベル数 match が 0 になると dname_remove_labels() で全ラベルが落ち、ルートに local_zone_transparent のゾーンが 1 つ作られる | doc/unbound.conf.5.in local-data の項: If local-data is configured that is not a subdomain of a local-zone, a transparent local-zone is configured. | verified | `local-zone-implicit-transparent-can-land-at-root` |
| B | 5.3 | local-zone-override は local-zone: が拒否する truncate を受理する | util/configparser.y server_local_zone_override は type を検証せず cfg_str3list_insert() するだけ。services/localzone.c lz_enter_override() が local_zone_str2type() を使うため、local-zone: の受理リストにない truncate と nodefault も通る | doc/unbound.conf.5.in L3486-: Use this localzone type, regardless the type configured for the local zone ...（使える type の範囲について記述がない） | verified | `local-zone-override-accepts-truncate` |
| B | 5.3 | local-zone-override は同名の local-zone が既に存在しないと fatal error になる | services/localzone.c lz_enter_override() が local_zones_find()（完全一致検索）で見つからなければ log_err("no local-zone for local-zone-override %s") を出して 0 を返す。lz_enter_overrides() は 0 で打ち切る | doc/unbound.conf.5.in L3486-: Override the local zone type for queries from addresses matching netblock. Use this localzone type, regardless the type configured for the local zone (both tagged and untagged) and regardless the type configured using access-control-tag-action. | verified | `local-zone-override-requires-exact-zone` |
| B | 5.3 | always_nodata は動作するが local-zone の type 一覧に載っていない | util/configparser.y server_local_zone が "always_nodata" を受理し、services/localzone.c local_zone_str2type() が local_zone_always_nodata に変換する | doc/unbound.conf.5.in L2910-L2925 の local-zone: の type 列挙: deny, refuse, static, transparent, redirect, nodefault, typetransparent, inform, inform_deny, inform_redirect, always_transparent, block_a, block_aaaa, block_a_wdata, block_aaaa_wdata, always_refuse, always_nxdomain, always_null, noview（always_nodata と always_deny を含まない）。ただし L3156 に .B always_nodata、L3162 に .B always_deny の個別説明はある | verified | `local-zone-type-always-nodata-works` |
| B | 5.4 | redirect ゾーンの CNAME ターゲットのワイルドカードは CNAME 以外の問い合わせでだけ展開され、CNAME で問うと * のまま返る | services/localzone.c local_data_answer()。alias を作り dname_is_wild() でターゲットを展開する分岐は qinfo->qtype != LDNS_RR_TYPE_CNAME のときだけ通る。CNAME の問い合わせは通常の redirect 分岐に落ち、オーナー名を問い合わせ名に差し替えるだけで、格納した rdata をそのまま返す | doc/unbound.conf.5.in redirect の項: As different from CNAME records that are used elsewhere, in the redirect type local-zone, it is supported that in the target of the record a wildcard label gets expanded to the query name, with for example: example.com. CNAME *.foo.net. gets expanded to www.example.com. CNAME www.example.com.foo.net.. | verified | `local-zone-redirect-cname-wildcard-not-expanded-for-cname-query` |
| B | 5.4 | redirect ゾーンでは CNAME と他の型の local-data を同居させられない | services/localzone.c local_zone_enter_rr()。redirect / inform_redirect で node->rrsets が既にある場合、追加が CNAME か既存が CNAME なら log_err して 0 を返す | doc/unbound.conf.5.in `redirect` の項: The CNAME record has to be with the zone name of the local-zone, and there can be one CNAME, not more. | verified | `local-zone-redirect-cname-with-other-data` |
| B | 6.4 | #name のときの既定ポートは tls-port: の設定値ではなくコンパイル時定数 853 である | util/net_help.c authextstrtoaddr() が UNBOUND_DNS_OVER_TLS_PORT (config.h.in で 853) を直接代入する。cfg->ssl_port（tls-port:）は services/listen_dnsport.c の待ち受け側と services/authzone.c でしか読まれず、forward-addr / stub-addr の解釈には一切関与しない | doc/unbound.conf.5.in forward-addr の項: If only '#' is used the default port is the configured tls-port. | verified | `forward-addr-name-ignores-tls-port-setting` |
| B | 6.4 | #name を付けるだけで宛先ポートが 853 になり、TLS かどうかは参照されない | util/net_help.c authextstrtoaddr()。@ が無く # がある経路で無条件に port = UNBOUND_DNS_OVER_TLS_PORT (853) を代入する。forward-tls-upstream に相当する cfg->ssl_upstream も dp->ssl_upstream もこの時点では見ていない | doc/unbound.conf.5.in forward-addr の項: If TLS is enabled, then you can append a '#' and a name, then it'll check the tls authentication certificates with that name. / If only '#' is used the default port is the configured tls-port. | verified | `forward-addr-name-implies-port-853` |
| C | 5.2 | 単一引用符で囲むと中の二重引用符が値として残り TXT が 1 つの character-string になる | util/configlexer.lex SQANY = [^\'\n\r\\]\|\\. なので " は普通の文字。sldns/str2wire.c rrinternal_get_quoted() が先頭の " を見て区切りを " に切り替える | doc/unbound.conf.5.in local-data の項: For record types such as TXT, use single quotes, as in: local-data: 'example. TXT "text"' | verified | `conf-lexer-single-quote-keeps-double-quote` |
| C | 5.2 | 二重引用符の中でエスケープした \" は TXT の引用符にならず値と語の区切りが壊れる | util/configlexer.lex は \\" を 2 文字のまま残す。sldns/str2wire.c rrinternal_get_quoted() は先頭が \\ なので引用と見なさず、区切りは "\n\t " のまま。sldns/parseutil.c sldns_parse_char() が後で \\" を " に解くので引用符が値に入る | doc/unbound.conf.5.in local-data の項: For record types such as TXT, use single quotes, as in: local-data: 'example. TXT "text"' | verified | `local-data-double-quote-escape-splits-txt` |
| C | 5.3 | block_aaaa は local-data があっても AAAA に NODATA を返す | services/localzone.c local_zones_answer() L1990 の条件が lzt != local_zone_block_aaaa を含むため local_data_answer() を通らない。local_zones_zone_answer() L1761 が qtype == AAAA に NOERROR/NODATA を返す | doc/unbound.conf.5.in L3115 .B block_aaaa の項 | verified | `local-zone-block-aaaa-ignores-local-data` |
| C | 5.3 | block_aaaa_wdata は local-data があればそれを返す | services/localzone.c local_zones_answer() L1990 の除外条件に block_aaaa_wdata が入っていないため local_data_answer() が先に効く。無ければ local_zones_zone_answer() L1761 で NODATA | doc/unbound.conf.5.in L3133 .B block_aaaa_wdata の項 | verified | `local-zone-block-aaaa-wdata-uses-local-data` |
| C | 5.4 | redirect ゾーンの CNAME のターゲットは、local-zone が覆っていても上流で解決される | services/localzone.c local_data_answer() は CNAME を qinfo->local_alias に入れて戻るだけ。daemon/worker.c worker_handle_request() が local_alias_shallow_copy_qname() で qname をターゲットに差し替え、キャッシュと mesh へ進む。local_zones_answer() は元の名前で 1 度呼ばれたきりで、iterator も local zone を参照しない | doc/unbound.conf.5.in redirect の項: The resolution proceeds with upstream DNS resolution, and that does not include the lookup in local zones. So the record is not able to point in local zones, but it can point to upstream DNS answers. | verified | `local-zone-redirect-cname-target-skips-local-zones` |
| C | 5.4 | redirect ゾーンの local-data は頂点と完全一致でなければ fatal error になる | services/localzone.c lz_enter_rr_into_zone()。z->type が local_zone_redirect / local_zone_inform_redirect のとき query_dname_compare(z->name, nm) != 0 で log_err して 0 を返す | doc/unbound.conf.5.in `redirect` の項: There may be no local data beneath the zone name. | verified | `local-zone-redirect-non-apex-data` |
| C | 5.5 | static ゾーンで CNAME だけを持つ名前に A を問うと、ターゲットが同じゾーンにあっても CNAME だけが返る | services/localzone.c local_data_answer() は local_data_find_type(ld, qtype, 1) を呼ぶ。alias_ok が立っているので、問われた型が無ければ CNAME の RRset を返す。redirect / inform_redirect 以外では local_encode() がその RRset 1 つだけで応答を作り、ターゲットを引く処理は無い | doc/unbound.conf.5.in redirect の項: CNAME resolution is supported only in type redirect local-zone, and in type inform_redirect local-zone. | verified | `local-data-cname-in-static-zone-not-chased` |
| C | 5.5 | local-data のワイルドカードは展開されず、* を含むリテラル名になる | services/localzone.c にワイルドカード照合が無い。dname_is_wild() の参照は local_zone_answer_cname() の CNAME ターゲット展開（L1593）だけで、オーナー名の照合には使われない。local_zones_zone_answer() L1728 のコメントも no additional section processing, cname, dname or wildcard processing と明記する。名前は sldns_str2wire_dname() でそのままワイヤ化され、* は 1 バイトのラベル内容として格納される | doc/unbound.conf.5.in L2935-L2938 および L3455-L3458: If you need more complicated authoritative data, with referrals, wildcards, CNAME/DNAME support, or DNSSEC authoritative service, setup a stub-zone for it as detailed in the stub zone section below. | verified | `local-data-wildcard-label-is-literal-name` |
| undecided | 6.5 | forward-no-cache: yes のとき 2 回目の同一問い合わせも上流へ出るか | iterator/iter_fwd.c read_forwards() が dp->no_cache = s->no_cache を設定する。宛先の決定には関与しない。キャッシュを経由するかどうかを外から見るには、上流が応答を返して初めてキャッシュに入る必要がある | doc/unbound.conf.5.in forward-no-cache の項: If enabled, data inside the forward is not cached. This is useful when you want immediate changes to be visible. | inconclusive | `forward-no-cache-repeat-query-hits-upstream` |
