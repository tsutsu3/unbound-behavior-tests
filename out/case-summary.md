<!-- generated by gen.py. DO NOT EDIT.
     unbound: 1.26.0 (release-1.26.0, a45da353)
     cases: 90a512e -->


# Case summary

## By category

| Category | Cases |
| --- | --- |
| A | 38 |
| B | 9 |
| C | 7 |
| D | 0 |
| undecided | 1 |
| Total | 55 |

## By status

| Status | Cases |
| --- | --- |
| verified | 54 |
| needs-test | 0 |
| inconclusive | 1 |

## Behaviours decided by two or more layers

| Section | Behaviour | Layers | Case |
| --- | --- | --- | --- |
| 5.2 | include: wildcards are read in sorted file name order, and the last file wins | glob expansion order + include expansion + config list build order (head insertion) | `conf-include-glob-processed-in-sorted-order` |
| 5.2 | An included file is placed at the include: position, and lines written after it win | include expansion + config list build order (head insertion) + duplicate handling at zone registration | `conf-include-later-line-wins` |
| 5.2 | With include: at the end, the included file beats the main configuration | include expansion + config list build order (head insertion) + duplicate handling at zone registration | `conf-include-position-not-file-decides` |
| 5.2 | After include-toplevel: a section must be opened again, or it is a syntax error | include expansion + section clause start detection | `conf-include-toplevel-closes-section` |
| 5.2 | The lexer does not resolve backslash escapes and hands them to the RR parser | config file lexer + RR owner name to dname conversion | `conf-lexer-backslash-not-resolved` |
| 5.2 | Forgetting the second argument of local-zone makes it consume the next line's directive name | config file lexer (state machine) + argument count in the grammar rule | `conf-lexer-missing-argument-eats-next-directive` |
| 5.2 | With single quotes the inner double quotes stay in the value, so the TXT is one character-string | config file lexer + rdata tokenisation of RR text | `conf-lexer-single-quote-keeps-double-quote` |
| 5.2 | An escaped \" inside double quotes does not become a TXT quote, and breaks the value and the word boundaries | config file lexer + rdata tokenisation of RR text + escape resolution | `local-data-double-quote-escape-splits-txt` |
| 5.2 | Inside a local-data value, ; starts a comment, not # | config file comment rule + rdata tokenisation of RR text | `local-data-semicolon-starts-rr-comment` |
| 5.3 | block_aaaa returns NODATA for AAAA even when local-data exists | answer generation per zone type + whether local-data is consulted | `local-zone-block-aaaa-ignores-local-data` |
| 5.3 | block_aaaa_wdata returns local-data when there is any | answer generation per zone type + whether local-data is consulted | `local-zone-block-aaaa-wdata-uses-local-data` |
| 5.3 | On a duplicate only the local-zone line is discarded; all the local-data goes into one zone | duplicate handling at zone registration + covering zone lookup for local-data | `local-zone-duplicate-keeps-both-local-data` |
| 5.3 | With duplicate local-zone lines, the type written last in the file remains | config list build order (head insertion) + duplicate handling at zone registration | `local-zone-duplicate-last-line-wins` |
| 5.3 | local-data without a local-zone creates one transparent zone at the shortest common parent domain | scan of uncovered local-data + common suffix computation and zone creation | `local-zone-implicit-transparent-at-shared-suffix` |
| 5.3 | local-data with no common parent domain puts the implicit transparent zone at the root | scan of uncovered local-data + common suffix computation and zone creation | `local-zone-implicit-transparent-can-land-at-root` |
| 5.3 | In an implicit transparent zone, a query for the same name with another type gets NODATA | implicit zone creation + answer generation per zone type | `local-zone-implicit-transparent-gives-nodata` |
| 5.3 | local-zone names ignore the trailing dot and letter case, so different spellings are duplicates of one zone | config list build order (head insertion) + zone name wire conversion and normalisation + duplicate handling at zone registration | `local-zone-name-trailing-dot-and-case-are-same-zone` |
| 5.3 | local-zone-override accepts truncate, which local-zone: rejects | type validation in the grammar + type conversion at override registration | `local-zone-override-accepts-truncate` |
| 5.3 | local-zone-override is a fatal error unless a local-zone with the same name already exists | exact-match zone tree lookup + insertion into the override tree | `local-zone-override-requires-exact-zone` |
| 5.3 | A deny zone at the root does not reach names under a more specific zone; the built-in zones keep answering | closest enclosing zone lookup + built-in zone registration | `local-zone-root-deny-keeps-builtin-zones` |
| 5.3 | always_nodata works but is missing from the local-zone type list | type validation in the grammar + answer generation per zone type | `local-zone-type-always-nodata-works` |
| 5.3 | ipset passes the grammar but fails at zone registration in builds without ipset support | type validation in the grammar + type conversion at zone registration | `local-zone-type-ipset-accepted-then-fails` |
| 5.4 | The CNAME target of a redirect zone is resolved upstream even when a local-zone covers it | local alias for the CNAME + resolution of the alias target | `local-zone-redirect-cname-target-skips-local-zones` |
| 5.4 | A wildcard CNAME target in a redirect zone is expanded only for non-CNAME queries; a CNAME query gets the literal * | query type check before alias creation + wildcard target expansion | `local-zone-redirect-cname-wildcard-not-expanded-for-cname-query` |
| 5.4 | In a redirect zone, a CNAME cannot coexist with local-data of other types | zone type check + adding an RRset to a local_data node | `local-zone-redirect-cname-with-other-data` |
| 5.4 | In a redirect zone, local-data that does not exactly match the apex is a fatal error | zone apex dname comparison + local-data zone registration | `local-zone-redirect-non-apex-data` |
| 5.5 | An IPv6 zone ID (%eth0) is rejected in AAAA RDATA | RR rdata to wire conversion + socket address construction (for contrast) | `local-data-aaaa-rejects-ipv6-zone-id` |
| 5.5 | local-data owner names are not distinguished by a trailing dot either, and an identical RR is ignored as a duplicate | RR owner name to dname conversion + RRset duplicate detection | `local-data-owner-trailing-dot-is-same-name` |
| 5.5 | local-data with the same name and type are merged into one RRset, in the order written | config list build order (head insertion) + RR insertion position in the RRset | `local-data-same-name-type-merges-rrset` |
| 5.5 | The TXT limit of 255 counts the wire length after escape resolution, not the input length | escape resolution + RR rdata to wire conversion | `local-data-txt-limit-is-wire-length` |
| 5.5 | local-data wildcards are not expanded; the name is a literal that contains * | RR owner name to dname conversion + zone data lookup (no wildcard processing) | `local-data-wildcard-label-is-literal-name` |
| 5.6 | nodefault removes the built-in zone, and queries go upstream | routing into the nodefault list + suppression of built-in zones + forwarding destination selection | `local-zone-nodefault-removes-builtin-zone` |
| 6.4 | unbound-checkconf validates the forward-addr address string itself | what checkconf checks + address parsing at startup | `checkconf-validates-forward-addr` |
| 6.4 | forward-addr accepts an IPv6 zone ID and turns it into sin6_scope_id | socket address construction + RR rdata to wire conversion (for contrast) | `forward-addr-accepts-ipv6-zone-id` |
| 6.4 | With #name the default port is the compile-time constant 853, not the tls-port: setting | address parsing + scope of the tls-port setting | `forward-addr-name-ignores-tls-port-setting` |
| 6.4 | Adding #name alone changes the destination port to 853, and TLS is never consulted | address parsing + TLS decision (separate layer, not consulted) | `forward-addr-name-implies-port-853` |
| 6.5 | forward-first does not change the destination, only the fallback behaviour | forwarder list construction + fallback decision | `forward-first-does-not-change-destination` |
| 6.5 | With forward-no-cache: yes, does a repeated identical query also go upstream? | forwarder list construction + cache control | `forward-no-cache-repeat-query-hits-upstream` |

## Behaviours decided by a single layer

| Behaviour | Layer | Case |
| --- | --- | --- |
| nodefault on a name that is not a built-in zone only warns, and checkconf succeeds | checkconf extra checks | `checkconf-warns-nodefault-without-builtin-zone` |
| A double quote inside a double-quoted value ends the value there | config file lexer | `conf-lexer-double-quote-terminates-early` |
| A quoted value cannot span lines (there is no line continuation syntax) | config file lexer | `conf-lexer-no-line-continuation` |
| There is no limit on the number of forward-addr entries | forwarder list construction | `forward-addr-many-entries-no-limit` |
| forward-addr @-1 is not an error and becomes 65535 | address parsing | `forward-addr-port-negative` |
| A forward-addr @port that does not start with a digit is rejected | address parsing | `forward-addr-port-non-numeric-rejected` |
| The forward-addr @port is not range-checked and is truncated to uint16 | address parsing | `forward-addr-port-overflow` |
| Trailing characters after the forward-addr @port number are silently ignored | address parsing | `forward-addr-port-trailing-garbage-ignored` |
| forward-addr @0 is accepted as port 0 | address parsing | `forward-addr-port-zero-accepted` |
| Omitting the local-data TTL gives 3600 | local-data RR string parsing | `local-data-default-ttl-3600` |
| local-data-ptr is rewritten into a PTR local-data string at config parse time | string rewrite at config parse time | `local-data-ptr-is-rewritten-to-local-data` |
| A TXT character-string of up to 255 wire bytes is accepted | RR rdata to wire conversion | `local-data-txt-255-bytes-ok` |
| A TXT character-string of 256 bytes is a configuration error | RR rdata to wire conversion | `local-data-txt-256-bytes-rejected` |
| local-data containing * matches only queries for the literal name * | RR owner name to dname conversion | `local-data-wildcard-label-matches-only-literally` |
| With no configuration at all, test. answers as a built-in static zone | built-in zone registration | `local-zone-builtin-test-zone-answers-locally` |
| truncate is a localzone_type but local-zone: does not accept it | type validation in the grammar | `local-zone-type-truncate-rejected-in-conf` |
| stub-addr goes through the same authextstrtoaddr() as forward-addr, so its port is truncated the same way | address parsing | `stub-addr-shares-port-parsing-with-forward-addr` |
